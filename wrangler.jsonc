/**
 * For more details on how to configure Wrangler, refer to:
 * https://developers.cloudflare.com/workers/wrangler/configuration/
 */
{
	"$schema": "node_modules/wrangler/config-schema.json",
	"name": "ari",
	"compatibility_date": "2026-06-05",
	"main": "./workers/app.ts",
	"observability": {
		"enabled": true
	},
	"upload_source_maps": true,
	"compatibility_flags": [
		"nodejs_compat"
	],
	"d1_databases": [
		{
			"binding": "DB",
			"database_name": "ari",
			"database_id": "3e5ba8f5-84dc-4bed-bccc-a2f093aaa8fc",
			"migrations_dir": "drizzle/migrations"
		}
	]
	/**
	 * Static Assets — uncomment when you install the `widget-embed` skill so
	 * that /widget.js (built into ./public) is served by the Worker.
	 * https://developers.cloudflare.com/workers/static-assets/binding/
	 */
	// "assets": { "directory": "./public/", "binding": "ASSETS" }
	/**
	 * R2 bucket — uncomment when you install the `r2-uploads` skill.
	 * Create it first: npx wrangler r2 bucket create ari-uploads
	 * https://developers.cloudflare.com/r2/
	 */
	// "r2_buckets": [ { "binding": "UPLOADS", "bucket_name": "ari-uploads" } ]
}